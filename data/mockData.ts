import { ZipCodeData } from '../types';

// This file is now deprecated as we fetch live data from the Census API.
// The empty object is retained to prevent import errors in files that still reference it.
export const westValleyData: Record<string, ZipCodeData> = {};
